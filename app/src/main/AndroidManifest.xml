<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      权限分阶段开启（见 text/plans/plan.md）：
      M1 番茄钟：FOREGROUND_SERVICE / FOREGROUND_SERVICE_SPECIAL_USE / POST_NOTIFICATIONS
                / SCHEDULE_EXACT_ALARM 或 USE_EXACT_ALARM
      M2 任务提醒：同上 + RECEIVE_BOOT_COMPLETED + VIBRATE
      M4 手机管控（本轮不做）：PACKAGE_USAGE_STATS / BIND_ACCESSIBILITY_SERVICE
                / SYSTEM_ALERT_WINDOW / REQUEST_IGNORE_BATTERY_OPTIMIZATIONS
      demo 阶段先不声明，避免申请未使用的敏感权限。
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tomato"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Tomato"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
